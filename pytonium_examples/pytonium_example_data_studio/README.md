# Pytonium Data Studio

A powerful data analysis application showcasing Pytonium's ability to handle **large datasets efficiently** using Python's data science stack.

## ✨ Features

### 📁 File Loading
- **Drag & drop** CSV or Excel files directly onto the app
- **File picker** for browsing
- Supports `.csv`, `.xlsx`, `.xls` formats

### 🔍 Instant Auto-Analysis
When you drop a file, you immediately get:

| Feature | Description |
|---------|-------------|
| **Dataset Overview** | Row count, columns, memory usage, missing values |
| **Column Types** | Auto-detected with color-coded badges |
| **Type Detection** | Numeric, String, Datetime, Categorical |
| **Statistics** | Unique counts, null counts per column |
| **Data Preview** | First 20 rows in a scrollable table |

### 📊 Smart Auto-Visualization
The app **automatically generates appropriate charts** based on your data types:

| Data Combination | Auto-Generated Chart |
|-----------------|---------------------|
| Datetime + Numeric | 📈 Time series line chart |
| Two Numeric columns | 🔵 Scatter plot (correlation) |
| Categorical + Numeric | 📊 Bar chart |
| Single Numeric | 📊 Histogram (distribution) |
| Categorical (< 10 values) | 🥧 Pie chart |

**No configuration needed** - it just analyzes your data and creates the right visualizations!

### 🎛️ Custom Charts
Build your own visualizations:
- Choose from 6 chart types
- Select X and Y columns
- Generated by matplotlib on the Python side

## 🏗️ Architecture: The Data Handle Pattern

This example uses a **smart data handle pattern** for efficiency:

```
JavaScript          Python (Pytonium)
    │                    │
    ├─ Load File ───────>├─ Load into pandas
    │<─ Return data_id ──┤  (stays in Python memory)
    │                    │
    ├─ Get Preview ─────>├─ Return subset
    │  (data_id, rows)   │  (not full data!)
    │                    │
    ├─ Generate Chart ──>├─ matplotlib renders
    │  (data_id, config) │  → Base64 image
    │<─ Return image ────┤
```

**Benefits:**
- ✅ Handle **large files** (GBs) without crashing the UI
- ✅ Only transfer **summaries and images**, not raw data
- ✅ **Multiple datasets** can stay loaded in memory
- ✅ **Efficient memory usage** - data stays in Python

## 🚀 The Pytonium Advantage

### In Electron
```javascript
// 1. Install native Node modules (hope they compile)
npm install better-sqlite3 node-xlsx

// 2. Deal with platform-specific builds
// 3. Limited library selection
// 4. Complex IPC for large data
```

### In Pytonium
```python
# Just Python!
import pandas as pd
import matplotlib.pyplot as plt

# Load 1GB CSV? No problem
df = pd.read_csv('huge_file.csv')

# Auto-detect column types
dtypes = detect_column_types(df)

# Generate chart
plt.figure(figsize=(10, 6))
df.plot(kind='line')
```

## 📦 Installation

```bash
pip install pandas matplotlib openpyxl numpy
```

That's it! No compilation, no platform-specific binaries.

## 🎯 Usage

1. **Run the app:**
   ```bash
   python main.py
   ```

2. **Drop a CSV or Excel file** onto the drop zone

3. **Instantly see:**
   - Dataset overview with statistics
   - Color-coded column types
   - Auto-generated visualizations
   - Data preview table

4. **Create custom charts** using the sidebar controls

## 🎨 Color-Coded Column Types

| Color | Type | Badge |
|-------|------|-------|
| 🟢 Green | Numeric | `NUMERIC` |
| 🔵 Blue | String | `STRING` |
| 🟠 Orange | Datetime | `DATETIME` |
| 🟣 Purple | Categorical | `CATEGORICAL` |

## 📂 File Structure

```
pytonium_example_data_studio/
├── main.py              # Application entry point
├── data_manager.py      # Efficient data handling with IDs
├── index.html           # UI with drag-drop and auto-viz
└── README.md           # This file
```

## 🔑 Key Technical Points

### Data Handle Pattern
```python
# Return ID instead of data
result = {
    "data_id": "abc123",     # Reference to data in Python
    "info": {...},            # Small summary
    "preview": {...}          # First 20 rows only
}

# Use ID for subsequent operations
preview = get_preview(data_id="abc123", start_row=0, rows=100)
chart = generate_chart(data_id="abc123", chart_type="line", ...)
```

### Smart Type Detection
```python
def detect_column_types(df):
    for col in df.columns:
        if is_datetime(df[col]):
            type = "datetime"
        elif is_numeric(df[col]):
            type = "numeric"
        elif nunique / total < 0.1:  # Less than 10% unique
            type = "categorical"  # Good for pie/bar charts
        else:
            type = "string"
```

### Auto-Chart Selection
```python
def suggest_charts(column_types):
    if datetime_cols and numeric_cols:
        suggest_line_chart()   # Time series
    if len(numeric_cols) >= 2:
        suggest_scatter()      # Correlation
    if categorical_cols and numeric_cols:
        suggest_bar_chart()    # Comparison
```

## 💡 Why This Matters

Building this in Electron would require:
- Native modules for data processing
- Complex IPC for transferring large datasets
- Limited visualization libraries
- Platform-specific builds

In Pytonium:
- **pandas** handles any size data
- **matplotlib** creates publication-quality charts
- **Data handle pattern** keeps UI responsive
- **Pure Python** - works everywhere

This is the power of Pytonium - **Python's unmatched data ecosystem** in a modern desktop app! 🐍📊
